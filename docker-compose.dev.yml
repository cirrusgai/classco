services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: classco
      POSTGRES_PASSWORD: classco
      POSTGRES_DB: classco
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
